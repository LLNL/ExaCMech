/**
 * @file ECMech_core.h
 * @brief Umbrella header for ExaCMech's foundational, model-independent definitions.
 *
 * Includes the generated build configuration (ECMech_config.h), the physical/math
 * constants and indexing conventions (ECMech_const.h), and the host/device portability
 * macros (ECMech_gpu_portability.h, ECMech_port.h). Most other headers in the library
 * include this rather than including those three headers individually.
 */

// -*-c++-*-

#ifndef ECMECH_CORE_H
#define ECMECH_CORE_H

#include "ECMech_config.h"

#include "ECMech_const.h"
#include "ECMech_gpu_portability.h"
#include "ECMech_port.h"

#endif // ECMECH_CORE_H
